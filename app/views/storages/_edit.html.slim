- provide(:button_text, "更新する") 

.modal-dialog.modal-lg
  .modal-content.modal--color
    .modal-body
      button.float-end.btn-close[data-bs-dismiss="modal"]
      br
      h1 保管場所編集
      = form_with model: @storage, url: user_storage_path(@user), local: true do |f|
        = render "users/shared/error_messages", resource: @storage
        = render "form", f: f